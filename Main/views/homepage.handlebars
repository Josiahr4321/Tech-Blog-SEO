<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles.css">
    <title>{{ pageTitle }}</title>
</head>
<body>
    <header>
        <img src="{{ bannerImageUrl }}" alt="TechExploro Banner">
        <h1>{{ siteName }}</h1>
        <p>{{ siteDescription }}</p>
    </header>
    
    <nav>
        <ul>
            {{#each navItems}}
            <li><a href="{{ url }}">{{ label }}</a></li>
            {{/each}}
        </ul>
    </nav>
    
    <section class="content">
        <h2>Tech Page</h2>
        <p>Explore the latest in technology trends, innovations, and more.</p>
        <!-- Your tech-related content goes here -->
    </section>
    
    <footer>
        <div class="social-media">
            {{#each socialMediaLinks}}
            <a href="{{ url }}">{{ platform }}</a>
            {{/each}}
        </div>
        <div class="subscribe">
            <a href="{{ newsletterUrl }}">Subscribe to Newsletter</a>
        </div>
    </footer>
</body>
</html>
